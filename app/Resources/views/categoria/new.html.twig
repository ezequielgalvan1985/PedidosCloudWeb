{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}
{% block body %}
    <h1>Categorium creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form,{'attr': {'novalidate': 'novalidate'}}) }}
        <input type="submit" class="btn btn-flat btn-primary"value="Guardar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('categoria_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
