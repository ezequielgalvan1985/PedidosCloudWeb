{% extends 'base.html.twig' %}

{% block body %}
<ol class="breadcrumb">
  <li><a href="{{path('homepage')}}">Inicio</a></li>
  <li ><a href="{{path('condicioniva_index')}}">Condicion Iva</a></li>
  <li class="active"><a href="#">Detalle </a></li>
</ol>
  
  
<div class="row">    
    <div class="col-xs-12 col-sm-9 col-lg-9">
    <div class="panel panel-default">
        <div class="panel-heading">Detalle Registro</div>
        <div class="panel-body">

        <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ condicioniva.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ condicioniva.nombre }}</td>
            </tr>
            <tr>
                <th>Alicuota</th>
                <td>{{ condicioniva.alicuota }}</td>
            </tr>
        </tbody>
    </table>

    
            
        
            {{ form_start(delete_form) }}
                <input type="submit" value="Eliminar" class="btn btn-flat btn-danger">
                <a href="{{ path('condicioniva_edit', { 'id': condicioniva.id }) }}" class="btn btn-flat btn-primary">Editar</a>
            {{ form_end(delete_form) }}
         </div>
    </div>
    </div>
    </div>
{% endblock %}
