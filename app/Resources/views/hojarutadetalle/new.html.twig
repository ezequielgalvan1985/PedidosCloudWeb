{% extends 'base.html.twig' %}

{% block body %}
    
    <h3>Datos de Hoja de ruta</h3>
    
    <h3>Detalle:</h3>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('hojarutadetalle_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
